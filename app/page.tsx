"use client";
import DashboardLayout from "@/components/layouts/dashboard-layout";

export default function Dashboard() {
  // const [sidebarIsOpen] = useAtom(sidebarIsOpenAtom);
  return (
    <DashboardLayout>
      <p>Dashboard page is here</p>
    </DashboardLayout>
  );
}
